#!/usr/bin/env node

//변수(variable)은 값을 저장할 때 사용하는 식별자이다.

//두 단계를 거친다.
//1. 변수를 선언(정의)한다.
//2. 변수에 값을 할당한다.

//선언, varname 이라는 변수를 선언했다.
let varname;
//값이 있지않아 undefined 자료형이 출력된다.
console.log(varname);

//할당, varname 에 100이란 값을 할당했다.
varname = 100;
console.log(varname);